# Dev-/CI-Abhaengigkeiten fuer die Korrektheits-Gates (pytest -m "not slow").
# Floor-Pins: kompatibel ueber die CI-Matrix Python 3.10-3.12. Die vollen
# physikalischen Mess-Laeufe (slow) brauchen dieselben Pakete, laufen aber
# lokal (siehe README "Reproduzieren").
numpy>=2.5.0
scipy>=1.11
pytest>=8.0
ruff>=0.6
